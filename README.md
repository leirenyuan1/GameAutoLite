# 🎮 GameAutoLite — 游戏自动化助手

基于 **OpenCV 模板匹配** + **贝塞尔曲线鼠标轨迹** 的 Windows 桌面游戏自动化工具。全中文 PyQt6 图形界面，无需任何编程知识即可使用。

## ✨ 功能特性

### 核心功能
- **图像识别匹配** — 截取游戏画面，使用 OpenCV `TM_CCOEFF_NORMED` 模板匹配算法，精准识别目标图案
- **防检测鼠标轨迹** — 四阶贝塞尔曲线 + 物理惯性速度曲线 + 路径微抖动，模拟真人操作手感
- **真实按压点击** — mouseDown → 随机停留 50~150ms → mouseUp，杜绝封号风险
- **可视区域选择** — 半透明全屏遮罩拖拽划定点击区域，所见即所得
- **截图即上传** — 内置屏幕截图工具，直接框选游戏画面作为识别模板
- **F8 全局热键** — 游戏内一键启停，无需切换窗口

### 任务管理
- **多任务队列** — 支持多个识别任务同时运行，按顺序循环扫描
- **可折叠任务卡片** — 支持展开/折叠/排序/删除动画
- **自动命名** — 新建卡片自动命名为"任务N"，重名自动校验
- **灵活延迟配置** — 每个任务独立设置识别后随机延迟 + 点击后冷却时间
- **自定义点击次数** — 可编辑下拉框，预设单击/双击，可填入1~9999自定义次数

### 点击区域
- **单区域模式** — 拖拽框选一个点击区域
- **多区域模式** — 勾选后可连续框选多个区域，引擎按面积加权随机点击
- **跟随识别图** — 勾选后点击匹配到的位置，无需手动框选区域

### 停止条件
- **任务执行次数** — 指定任务执行 N 次后自动停止
- **运行时间限制** — 运行时间达到 N 分钟后自动停止
- **识别指定图片** — 识别到特定图片后自动停止
- **无匹配超时** — N 分钟无匹配后自动停止

### 悬浮窗
- **最小化时显示** — 最小化主窗口后显示悬浮状态卡片
- **始终显示** — 可选始终显示悬浮窗
- **透明度调节** — 30%~100% 可调
- **鼠标穿透** — 开启后鼠标事件穿透到下层窗口，不干扰游戏操作
- **拖拽移动** — 支持拖拽悬浮窗到任意位置

### 配置管理
- **配置自动保存** — 关闭时自动保存配置，下次启动恢复全部配置
- **方案导入导出** — 将任务配置导出为 `.galt` 文件，随时导入恢复
- **方案嵌入图片** — 导出时图片 Base64 嵌入 .galt 文件，导入时智能检测还原
- **停止条件导出** — 方案导出支持保存停止条件配置

### 其他
- **全局设置** — 图像扫描间隔、鼠标移动/点击速度可调（0.5X~2.0X）
- **开始后自动最小化** — 勾选后启动引擎自动最小化，停止时自动恢复
- **自动申请管理员权限** — 启动时自动请求（keyboard 全局热键需要）
- **窗口可调整大小** — 支持拖拽调整窗口大小

## 🛠 技术栈

| 组件 | 用途 |
|------|------|
| **PyQt6** | 图形界面框架 |
| **OpenCV + mss** | 屏幕截图与模板匹配 |
| **pywin32** | Windows API 鼠标控制 |
| **keyboard** | 全局热键注册 |

## 📁 项目结构

```
GameAutoLite/
├── main.py                # 程序入口
├── main_window.py         # 主窗口逻辑
├── engine.py              # 引擎调度线程
├── config_manager.py      # 配置持久化与方案导入导出
├── hotkey_manager.py      # F8 热键管理
├── image_engine.py        # 截图 + OpenCV 模板匹配
├── mouse_controller.py    # 贝塞尔曲线移动 + 真实按压点击
├── overlay_selector.py    # 全屏遮罩拖拽选区工具
├── icon.ico               # 软件图标
├── requirements.txt       # Python 依赖清单
│
├── styles/                # 样式模块
│   ├── colors.py          # 颜色常量
│   ├── qss.py             # 全局 QSS 样式
│   └── checkbox_style.py  # 自定义勾选框样式
│
├── widgets/               # 组件模块
│   ├── task_card.py       # 可折叠任务卡片
│   ├── sidebar.py         # 左侧边栏导航
│   ├── floating_widget.py # 悬浮状态卡片
│   └── nav_item.py        # 导航菜单项
│
└── pages/                 # 页面模块
    ├── task_list_page.py      # 任务列表页
    ├── stop_conditions_page.py # 停止条件页
    ├── settings_page.py       # 全局设置页
    └── utils.py               # 页面工具函数
```

## 🚀 快速开始

### ⚠️ 注意

运行软件后（无论是源码运行还是 exe），首次启动会在同目录下生成：
- `config.json` — 配置文件，保存你的任务设置和全局选项
- `screenshots/` — 存放截图上传的识别模板图片

这些文件由程序自动管理，不需要手动创建。

### 方式一：直接运行（推荐）

从 [Releases](../../releases) 页面下载：
- `GameAutoLite.exe` — 单文件，双击即用
- `GameAutoLite-folder.zip` — 解压后运行，启动更快

**推荐**：如果不嫌解压，下载文件夹版（GameAutoLite-folder.zip）体验更佳。

如点击无效，请右键选择「以管理员身份运行」。

### 方式二：从源码运行

```bash
# 1. 克隆仓库
git clone https://github.com/leirenyuan1/GameAutoLite.git
cd GameAutoLite

# 2. 安装依赖
pip install -r requirements.txt

# 3. 运行
python main.py
```

### 打包为 exe

```bash
# 文件夹形式（启动快）
pyinstaller --windowed --icon=icon.ico --name=GameAutoLite --add-data "icon.ico;." main.py

# 单文件形式（便于分发）
pyinstaller --onefile --windowed --icon=icon.ico --name=GameAutoLite --add-data "icon.ico;." main.py
```

## 📖 使用说明

1. **添加任务** — 点击「＋ 添加任务」，新建一张任务卡片
2. **上传识别图** — 点击「🖼 上传识别图」选择本地图片，或点击「📷 截图上传」框选屏幕区域
3. **设定点击区域** — 点击「⊕ 设定」，在游戏画面上拖拽划定要点击的位置（或多选模式框选多处）
4. **调整匹配精确度** — 滑块设置 50%~99%，越高越精确但越难匹配
5. **配置延迟** — 设置识别后的随机延迟范围（默认 200~500ms）
6. **设置停止条件**（可选） — 切换到「停止条件」页面，按需配置自动停止规则
7. **启动** — 点击绿色「▶ 开始」按钮或按 F8，引擎开始循环扫描
8. **停止** — 点击红色「⏹ 停止」按钮或按 F8

> 💡 提示：如点击无效，请右键本软件选择「以管理员身份运行」

## 🔒 防检测设计

- 点击落点在目标区域内 **均匀随机取点**，绝不取中心
- 鼠标移动使用 **四阶贝塞尔曲线**，控制点有 ±120px 随机偏移产生自然弧度
- 路径叠加 **±1~2px 逐点抖动**，模拟人手微颤
- 移动耗时按距离动态分配（短距 150~300ms，长距 400~650ms）
- 点击拆分为 **按下 → 随机延迟 → 松开**，模拟真实点击停留

## 📝 版本历史

### v3.0
- 新增停止条件系统（任务执行次数/运行时间/识别指定图片/无匹配超时）
- 新增悬浮窗功能（最小化显示/始终显示/透明度调节/鼠标穿透）
- 新增点击区域多选模式
- 新增任务卡片自动命名 + 重名校验
- 新增自动申请管理员权限
- 新增任务卡片折叠/展开/删除/排序动画
- 新增窗口支持拖拽调整大小
- 新增方案导入时任务名称自动去重
- 新增方案导出支持停止条件配置
- 图像扫描间隔默认值调整为 200ms
- 点击后冷却时间上限调整为 999999ms
- 修复截图/选取区域时窗口概率性异常的问题
- 全新蓝天白云风格 UI，左右双栏布局
- 代码结构重构为模块化（styles / widgets / pages）

### v2.0
- 新增全局设置页面（扫描间隔、鼠标速度可调）
- 新增点击区域为识别图功能
- 新增自定义点击次数（可编辑下拉框）
- 新增开始后自动最小化功能
- 新增方案导出时嵌入图片，导入时智能检测还原
- 新增导入方案可选（替换/新增）

### v1.2
- 修复截图窗口按 Esc 无法退出的问题
- 修复最小化窗口后 F8 热键和引擎异常的问题
- 新增方案导出/导入功能（.galt 文件）
- 新增软件图标

### v1.1
- 高 DPI 缩放下坐标偏移修复
- 中文路径图片加载支持
- 点击后冷却功能
- 截图上传功能
- exe 配置持久化

## 致谢

感谢以下工具与 AI 模型在本项目开发过程中提供的支持：

- **开发环境与工具**：VS Code, Claude Code, CC Switch
- **AI 辅助编程与对话**：Xiaomi MiMo, Claude, DeepSeek, Gemini, 豆包
